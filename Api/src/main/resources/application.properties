spring.application.name=ApiEjecucion

# Configuración del servidor
server.port=8080

# Configuración de logging
logging.level.root=INFO
logging.level.com.gymor=DEBUG

# Configuración de serialización JSON
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=America/Santiago

# Configuración de mensajes de error
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=on_param
server.error.include-exception=false

# Configuración de base de datos (cuando se implemente)
# spring.datasource.url=jdbc:postgresql://localhost:5432/gymor
# spring.datasource.username=gymor_user
# spring.datasource.password=gymor_pass
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true